# 국내여비 실비정산 도우미 — PRD (v0.2)

- 작성일: 2026-07-20 (v0.2: 메뉴/화면 상세 스펙 및 검증 규칙 반영)
- 대상 규정: `r1/여비규정.pdf` (한국해양과학기술원 여비규정, 2026.05.18 개정 시행본, 전 22p)
- 범위: **로컬 단일 사용자 프로토타입** (사내 다중 사용자 배포는 향후 확장 옵션으로만 기록)
- 진행 방식: **이번 업데이트는 PRD 확정까지만.** 실제 구현(UI 프로토타입 → OCR/검증 로직 → PDF 생성 → 이메일 발송)은 다음 대화에서 단계별로 진행.

---

## 1. 배경 및 목적

출장자가 국내 출장 후 조식·교통·숙박 등 실비 항목을 정산할 때, 매번 사람이 여비규정 조항과
한도를 대조해가며 영수증을 검토해야 하는 번거로움이 있다. 이 도구는 다음을 자동화한다.

1. 출장 기본정보(경로) 입력
2. 실비 항목(조식/교통/숙박) 영수증을 **사진 촬영 또는 업로드**로 수집
3. 멀티모달 LLM(OpenRouter `google/gemma-4-31b-it:free`, 혼잡 시 `gemma-4-26b-a4b-it:free`로 폴백)에
   영수증 이미지를 직접 보내 텍스트(OCR) 추출 — 최초 계획한 Google Vision은 프로젝트 결제 활성화 이슈로
   제외, 6장 Phase 2 참고
4. 여비규정을 학습한 에이전트가 OCR 결과를 규정/검증 규칙과 대조해 **항목별 적합 여부 판정**
5. 최종 실비정산 금액을 자동 산출하고, 통합 정산 문서(PDF)로 출력

---

## 2. 확정된 설계 전제

| 항목 | 결정 사항 | 비고 |
|---|---|---|
| 배포 형태 | 로컬 단일 사용자 프로토타입 (브라우저 UI + 로컬 백엔드, `localhost`) | 사내 배포는 9장 참고, 지금 범위 아님 |
| 직급 입력 | **받지 않음** | 항목별 한도는 아래 3장의 고정값(전 직급 공통)을 적용 |
| 최종 확정 방식 | 런타임에 사람이 재확인하지 않음. 에이전트/검증 규칙 산정값을 그대로 최종값으로 사용 | 배포 전 회귀 테스트(Phase 5)로 신뢰도 확보 |
| UI 디자인 | **애플 스타일** (아래 4장 참고) | 라운드 카드, 여백 중심, 시스템 폰트 |
| 교통 하위메뉴 범위 | **선박, 항공만** (철도/자동차는 이번 스코프 아님) | 등급 제한 검증이 필요한 교통수단만 우선 대상 |
| 조식 시간 인정 기준 | **05:00:00 ~ 10:00:59** | "10시를 조금 넘겨도 허용"으로 해석 |
| 숙박비 1박 상한 | **120,000원** (사용자 확정값 최우선 적용) | 규정 PDF 별표1 추정치(80,000원)와 차이 있음 — 3장에 불일치 기록, Phase 0에서 최종 재확인 권장 |
| 이메일 발송 기능 | 요구사항으로 채택하되 **향후 단계**(SMTP 설정 등 추가 구성 필요) | 지금은 PRD 반영까지만 |

---

## 3. 규정/검증 기준 요약 (구조화 룰테이블 대상)

에이전트가 참조할 핵심 조항과, 이번에 새로 확정된 세부 검증 규칙을 함께 정리한다. **금액·시간·등급 같은
수치·기준값은 LLM이 매번 새로 판단하지 않고, 아래 표를 코드화한 룰테이블에서 결정론적으로 조회**하며,
LLM(규정 학습 에이전트 + 실비 판정 에이전트)은 OCR 텍스트에서 품목/장소/등급 등 정성적 정보를 읽어내고
설명 문구를 만드는 역할을 담당한다.

| 항목 | 관련 조항(근거) | 확정 기준 | 불인정/부분인정 처리 |
|---|---|---|---|
| **조식** | 별표 제1호 비고5 (정정, 아래 각주 참고) | **출장경로(출발/경유/도착) 전체 중 하나와 장소 일치 필요** / **영수증 날짜가 출장기간(출발~도착일) 안에 있어야 함** / 금액 상한 **15,000원** / 인정 시간 **05:00:00~10:00:59** / 영수증 1장만 인정 / 품목에 비닐봉투·봉투값·주류·담배·식사 외 물품 포함 불가 | 금액 초과는 **부분인정**(15,000원까지만 인정), 그 외(품목 미확인/장소 불일치/기간 밖 날짜/시간 초과/불인정 품목 포함/2장 이상 첨부)는 **전체 불인정** |
| **교통 - 선박** | 제12조, 제13조, 별표 제1호 | **선박 관련 영수증인지 확인**(숙박/조식 영수증 오인식 방지) / 1등석 불인정(일반석만 인정) / **출장경로 전체 중 하나와 장소 일치 필요** / **영수증 날짜가 출장기간 안에 있어야 함** | 업체유형 불일치·등급 미충족·장소 불일치·기간 밖 날짜 시 **전체 불인정** |
| **교통 - 항공** | 제13조③, 별표 제1호 | **항공 관련 영수증인지 확인** / 비즈니스/퍼스트 클래스 불인정(이코노미만 인정) / **출장경로 전체 중 하나와 장소 일치 필요** / **영수증 날짜가 출장기간 안에 있어야 함** | 업체유형 불일치·등급 미충족·장소 불일치·기간 밖 날짜 시 **전체 불인정** |
| **숙박** | 제10조③, 제14조②⑤, 별표 제1호 | 영수증/receipt 문구 필수 포함 / **영수증 날짜가 출장기간 안에 있어야 함** / 거래장소가 도착지 또는 경유지와 일치 필요 / 1박 상한 **120,000원** | 영수증 미확인·기간 밖 날짜·장소 불일치는 **전체 불인정**, 금액 초과는 **부분인정**(120,000원까지만 인정) |

> 조식·교통은 장소 검증 시 **출장경로 전체(출발지 포함)** 를 후보로 쓰고, 숙박은 기존 스펙대로
> **도착지·경유지만**(출발지 제외) 후보로 쓴다 — 조식/교통은 출발 당일 아침 식사나 출발지에서
> 타는 교통편도 있을 수 있지만, 숙박은 통상 출발지에서 묵을 이유가 없다고 보아 원래 스펙을 유지했다.

> ⚠️ **열린 이슈**: 숙박비 상한을 이번에 120,000원으로 확정했으나, `r1/여비규정.pdf` 별표 제1호 텍스트 추출
> 결과로는 80,000원으로 추정되었다. 두 값이 다른 이유(내부 별도 지침 존재 여부, PDF가 구버전일 가능성 등)를
> Phase 0에서 원본 문서와 함께 재확인하는 것을 권장한다. 지금은 사용자가 확정한 **120,000원을 우선 적용**한다.
>
> **정정 (Phase 3, 2026-07-20)**: 규정을 조항 단위로 구조화하고 검색 에이전트로 대조해보니, 제29조(식비)는
> 제3장 국외여비 소속 조항으로 국내 조식과 무관했다. "출장 첫날 조식비 미지급, 영수증 증빙 시 예외" 규정은
> **별표 제1호 비고5)에만 명시**되어 있다. `rules/domestic_travel_rules.json`의 `breakfast.regulation_ref`도
> 이에 맞춰 갱신 필요(현재 "제29조① / 별표 제1호 비고5"로 되어 있어 다음 손볼 때 "별표 제1호 비고5"로 단순화할 것).

---

## 4. 메뉴 구성 및 UI 스펙

### 4.1 디자인 원칙 (애플 스타일)

- 큰 라운드 코너 카드(구석 반경 큼), 은은한 그림자, 넉넉한 여백
- 시스템 폰트 우선 (`-apple-system, BlinkMacSystemFont` 계열), 굵기 대비로 위계 표현
- 아이콘 + 라벨 조합의 정사각형 카드 (iOS 위젯/앱 아이콘 그리드 느낌)
- 절제된 색상: 배경은 무채색, 포인트 컬러 1개(시스템 블루 계열)만 강조에 사용
- 라이트/다크 모드 모두 대응
- 큰 터치 영역 — 카메라 촬영이 모바일 브라우저에서도 쓰기 편하도록 버튼을 크게

### 4.2 홈 화면

**출장정보 입력 (상단)**

- 출장경로: 리스트 형태로 구성, 각 행은 "구분 배지(출발/경유/도착) + 일자 + 장소"
  - 1행 고정: 출발일자 + 출발지
  - N행 (0개 이상, `+` 버튼으로 추가): 경유일자 + 경유지
  - 마지막행 고정: 도착일자 + 도착지
  - `+` 버튼으로 경유지를 순서대로 추가/삭제 가능한 동적 리스트

**실비 항목 카드 (하단, 3개)**

- 조식 / 교통 / 숙박 각각 아이콘을 포함한 정사각형 카드 버튼
  - 조식: 커피컵/식사 아이콘
  - 교통: 이동수단(배/비행기) 아이콘
  - 숙박: 침대/건물 아이콘
- 카드에는 현재까지 등록된 건수·잠정 인정금액을 작게 배지로 표시(예: "2건 · 24,000원")
- 카드 탭 시 각 세부 메뉴로 이동

### 4.3 조식 메뉴

1. **입력**: 사진 선택 또는 카메라 촬영 (1장만 — 2장 이상 첨부 자체를 UI에서 막을지, 첨부는 허용하고 분석 단계에서 거부할지는 구현 시 결정. 스펙상 최종 검증 규칙에는 "2장 이상 첨부 시 불인정"이 포함되어 있으므로 최소한 검증 로직에는 반드시 넣는다.)
2. **분석 화면**: 촬영/선택한 사진 미리보기 + OCR 인식 결과(상호명, 일시, 금액, 품목) 표시
3. **검증 순서 및 메시지** (앞선 항목에서 실패하면 다음 항목은 검사하지 않고 즉시 실패 메시지 표시 — fail-fast):

   | 순서 | 검증 내용 | 실패 시 메시지 | 처리 |
   |---|---|---|---|
   | 1 | 품목 확인 가능 여부 | "품목 확인이 되지 않는 영수증은 증빙이 되지 않습니다" | 전체 불인정 |
   | 2 | 시간 확인 (출장 당일 05:00:00~10:00:59) | "정산 인정기준 시간을 초과하였습니다" | 전체 불인정 |
   | 3 | 메뉴 확인 (비닐봉투/봉투값/주류/담배/식사 외 물품 포함 여부) | "불인정항목이 포함된 영수증으로 확인됩니다. 해당 영수증은 정산처리가 되지 않습니다" | 전체 불인정 |
   | 4 | 영수증 매수 (2장 이상 첨부) | "다수 영수증의 합산 청구는 불가합니다" | 전체 불인정 |
   | 5 | 금액 상한(15,000원) | "최대 15,000원만 정산됨을 안내드립니다" | **부분인정**(15,000원까지 인정, 나머지 거부는 아님 — 진행 가능) |

   > 구현 시 참고: 4번(매수 확인)은 논리상 1~3번보다 먼저 수행하는 것이 자연스러울 수 있으나(여러 장이면
   > 애초에 단일 영수증으로 분석하기 어려움), 이번 스펙은 사용자가 지정한 1→5 순서를 그대로 따른다. 순서를
   > 바꾸고 싶으면 구현 단계에서 다시 논의.

### 4.4 교통 메뉴

1. **하위 선택**: 선박 / 항공 중 택 1
2. **입력**: 사진 선택 또는 카메라 촬영
3. **분석 화면**: 사진 미리보기 + OCR 인식 결과(등급/좌석 클래스, 금액, 구간) 표시
4. **검증**:
   - 선박 선택 시: **1등석이면 불인정** → "제출하신 서류는 국내여비 지급표를 충족하지 않습니다" (전체 불인정)
   - 항공 선택 시: **비즈니스/퍼스트 클래스면 불인정** → "제출하신 서류는 국내여비 지급표를 충족하지 않습니다" (전체 불인정)
   - 등급 조건을 만족하면(선박 일반석, 항공 이코노미) 영수증 금액 그대로 실비 인정 (별도 상한 없음)

### 4.5 숙박 메뉴

1. **입력**: 사진 선택 또는 카메라 촬영
2. **분석 화면**: 사진 미리보기 + OCR 인식 결과(상호명/거래장소, 일자, 금액) 표시
3. **검증 순서 및 메시지**:

   | 순서 | 검증 내용 | 실패 시 메시지 | 처리 |
   |---|---|---|---|
   | 1 | 영수증 여부 확인 ("영수증"/"receipt" 등 문구 포함 필수) | "제출하신 서류는 영수증이 아닙니다. 확인후 다시 제출하여 주세요" | 전체 불인정 |
   | 2 | 거래장소가 출장정보의 도착지 또는 경유지와 일치 | "숙박장소가 출장장소와 일치하지 않습니다. 확인후 다시 제출하여 주세요" | 전체 불인정 |
   | 3 | 1박 상한(120,000원) | "최대 120,000원만 정산됨을 안내드립니다" | **부분인정**(120,000원까지 인정) |

   > 사용자가 준 2번 메시지 원문에 "영수증에 제출하신 서류는 영수증이 아닙니다"라는 중복/어색한 표현이
   > 있어 "숙박장소가 출장장소와 일치하지 않습니다. 확인후 다시 제출하여 주세요"로 다듬었다. 문구를 원문
   > 그대로 유지하고 싶다면 구현 단계에서 알려주면 반영.

### 4.6 결과 출력

1. **문서 생성**: 출장정보 + 조식/교통/숙박에서 인정된(또는 부분인정된) 항목들을 합산해 하나의 정산 문서로 생성
2. **문서 구성**: 항목별(조식/교통/숙박) 합계와 세부내역을 표로 정리 + 첨부된 증빙 영수증 이미지를 함께 묶어서 **PDF**로 제공
3. **결과 받기**:
   - 화면에서 PDF 다운로드 (필수)
   - **이메일로 받기** (선택, 향후 단계 — SMTP 계정/발신 정보 등 추가 설정 필요)

---

## 5. 시스템 구성 (로컬 프로토타입 아키텍처)

```
[브라우저 UI]  ──HTTP(localhost)──  [로컬 백엔드 서버]
  - 출장정보(경로) 입력 폼               - REST API
  - 조식/교통/숙박 카드 + 세부 화면      - 영수증 OCR (OpenRouter 멀티모달: gemma-4-31b-it:free →
  - 카메라 촬영 / 파일 업로드              429 시 gemma-4-26b-a4b-it:free 폴백)
  - 검증 결과/안내 메시지 표시           - 규정 학습 에이전트 (RAG, r1/ PDF 기반)
  - PDF 다운로드 / (향후) 이메일 발송    - 실비 판정 에이전트 (LLM: gemma-4-31b-it:free via OpenRouter)
                                        - 검증 룰테이블 (3장 표 기반, 구조화 JSON)
                                        - PDF 생성 모듈
                                        - (향후) 이메일 발송 모듈 (SMTP)
                                        - 로컬 DB (SQLite, DATABASE_URL 이미 .env에 설정됨)
```

### 5.1 에이전트 2종의 역할 분리

| 에이전트 | 역할 | 출력 |
|---|---|---|
| **규정 학습 에이전트** (RAG) | `r1/여비규정.pdf` 조항 원문을 임베딩/색인하고, 질의에 관련 조항 원문을 검색해 반환 | 관련 조항 텍스트 + 조번호 |
| **실비 판정 에이전트** (LLM 추론) | OCR 결과 + 3장 룰테이블(수치·조건) + 규정 학습 에이전트 검색 결과를 입력받아, 4장의 검증 순서대로 항목별 적합여부·인정금액·안내 메시지를 산출 | 판정 결과 JSON (통과/실패 단계, 인정금액, 메시지, 근거) |

숫자(상한액, 시간 범위)와 등급 판정 로직은 룰테이블에서 결정론적으로 계산하고, LLM은 OCR 텍스트에서
"품목이 식사인지/불인정 품목이 섞여있는지", "좌석 등급이 무엇인지", "거래장소 문자열이 출장지와
일치하는지" 같은 자연어 판단에 사용한다.

---

## 6. 단계별 로드맵

각 단계는 이전 단계 완료를 전제로 하며, 단계마다 완료 기준을 통과해야 다음으로 넘어간다.
(v0.2 기준: **지금은 Phase 0 착수 전, PRD 확정 단계.** 실제 구현은 다음 대화에서 진행)

### Phase 0 — 준비 및 규정/룰테이블 구조화 ✅ 완료 (2026-07-20)
- [x] `OPENROUTER_API_KEY` 발급 및 `.env`에 입력 — 인증 정상 확인(무료 모델 업스트림 429는 별개 이슈로 기록)
- [x] 숙박비 상한(120,000원 vs PDF 추정 80,000원) 불일치 재확인 — 120,000원 최종 확정
- [x] 조식/교통/숙박 기준을 `rules/domestic_travel_rules.json`으로 코드화
- [x] `.env`를 `.gitignore`에 등록

### Phase 1 — UI 프로토타입 ✅ 완료 (2026-07-20)
- 홈 화면: 출장경로 입력(출발/경유 N개/도착) + 조식/교통/숙박 카드, 애플 스타일 적용
- 조식/교통/숙박 각 세부 화면: 사진 선택/카메라 촬영 UI, 하위메뉴(교통: 선박/항공)
- 아직 OCR/판정 없이 이미지 저장 및 화면 흐름만 구현

**구현 스택**: Next.js 16 (App Router, Turbopack) + TypeScript + Tailwind CSS v4 + Prisma 6 + SQLite
(`.env`의 `DATABASE_URL="file:./dev.db"` 컨벤션에 맞춤). Prisma는 7.x가 최신이지만 드라이버 어댑터
기반으로 구조가 크게 바뀌어 로컬 프로토타입에는 과도하다고 판단, 안정적인 6.x로 고정.

**주요 파일**: `prisma/schema.prisma`(Trip/TripStop/Receipt 모델), `src/app/api/trips`,
`src/app/api/receipts`(이미지 업로드 → `public/uploads/<tripId>/<category>/` 저장),
`src/app/trip/[id]/{breakfast,transport,lodging}/page.tsx`, `src/components/ReceiptManager.tsx`
(사진 선택/카메라 촬영 공통 위젯).

**검증 완료**: 홈 → 출장 생성 → 허브 → 조식/교통/숙박 3개 화면 → 실제 이미지 업로드(각 항목 1건)까지
curl 기반 end-to-end 테스트 통과. 업로드 API의 잘못된 요청 처리(500→400) 버그 1건 발견 후 즉시 수정.
카메라 촬영 자체(모바일 브라우저의 실제 카메라 UI)는 헤드리스 환경에서 검증 불가 — 사용자가 브라우저에서
직접 확인 필요.

### Phase 2 — OCR 연동 ✅ 완료 (2026-07-20)
- 최초 계획은 Google Vision이었으나, 실제 호출 시 GCP 프로젝트(#1038906444326)에 결제(billing)가
  활성화되어 있지 않아 403 오류 발생 — 사용자 확인 결과 결제 활성화는 보류하기로 하고, **이미 설정된
  멀티모달 LLM으로 OCR을 대체**하기로 결정
- 사용자가 처음 제안한 `x-ai/grok-imagine-image-quality`는 텍스트→이미지 생성 전용 모델로 확인되어
  (이미지에서 텍스트를 읽는 기능 없음) 채택하지 않음
- 최종: `src/lib/receiptOcr.ts`에서 OpenRouter 멀티모달 채팅 API로 영수증 이미지를 `gemma-4-31b-it:free`에
  직접 전송해 텍스트를 옮겨 적게 함. 이 모델이 업스트림 혼잡(429)일 경우 형제 무료 모델
  `gemma-4-26b-a4b-it:free`로 자동 1회 폴백하고, 실제 처리한 모델명을 `Receipt.ocrModel`에 기록해 투명성 확보
- OCR 결과 원문에서 상호명/일시/금액 후보를 뽑는 `src/lib/receiptHints.ts` 정규식 휴리스틱은 그대로 유지
  (Vision이든 LLM이든 "원문 텍스트 한 덩어리"만 반환하면 동일하게 재사용 가능하도록 설계)
- Prisma `Receipt`에 `ocrStatus/ocrText/ocrAmountGuess/ocrDateGuess/ocrMerchantGuess/ocrModel` 필드 추가,
  `ReceiptManager` UI에서 썸네일 탭 시 인식 결과(상호명/일시/금액 + 원문 텍스트 펼쳐보기) 표시

**검증 완료**: PIL로 만든 합성 영수증 3장(조식/숙박/선박 각 1장, 한글 텍스트 포함)으로 end-to-end 테스트.
1차 모델이 매 테스트마다 429였음에도 폴백이 정확히 동작해 상호명·금액·일시를 정확히 인식(스타벅스
4,500원, 신라스테이 115,000원, 선박 45,000원 · "좌석등급: 1등실" 문구까지 인식 — Phase 4 등급 검증에
바로 쓸 수 있는 원문 확보). 업로드/DB/UI 파이프라인 전체가 실제 이미지로 정상 동작함을 확인.

> Google Vision 관련 `.env`의 `GOOGLE_VISION_API_KEY`/`GOOGLE_GENAI_API_KEY`는 더 이상 코드에서 쓰이지
> 않음 — 삭제하지 않고 남겨두었으니 필요 없다면 정리해도 무방.

### Phase 3 — 규정 학습 에이전트(RAG) 구축 ✅ 완료 (2026-07-20)
- 조항 수 45개 내외(+별표 7종)로 코퍼스가 작아, 별도 벡터DB/임베딩 API 없이 **조항 단위 구조화 +
  TF-IDF 코사인 유사도 검색**으로 구현 (임베딩 API 의존성을 늘리지 않기 위한 선택)
- `r1/여비규정.pdf` → 조항별 구조화 JSON `rules/regulation_articles.json` (총 57개 청크: 조항 50개 +
  별표 7종), 정규식으로 "제N조(제목)" 패턴과 장(chapter) 헤딩을 파싱해 조번호/제목/장/본문 필드로 분리
- `src/lib/regulationSearch.ts`: 한글은 조사/어미가 붙는 교착어라 단순 공백 분리가 잘 안 맞아, 한글
  연속구간을 문자 2-gram으로 쪼개는 방식으로 토큰화(형태소 분석기 없이도 꽤 잘 작동) 후 TF-IDF 코사인
  유사도로 topK 반환
- `GET /api/regulation-search?q=...&topK=3`로 단독 조회 가능

**검증 완료**: 대표 질의 5개 테스트
- "숙박비 한도 초과 시 예외 사유" → 1위 **제14조**(정확히 해당 조항) ✅
- "조식은 언제 지급되나요 출장 첫날" → 1위 **별표 제1호**(정확) ✅ — 이 과정에서 **제29조는 국외여비
  조항이라 국내 조식과 무관함을 발견**, 3장 표와 `rules/domestic_travel_rules.json`을 정정(위 3장 각주 참고)
- "여비 정산 신청 절차" → 1위 **제11조**, 2위 **별표 제7호**(둘 다 정확) ✅
- "교통비 운임 지급 기준" → 제13조(국내)가 2위, 국외 조항(제23조)이 1위로 나옴 — 알려진 한계로 기록(아래)
- "선박 1등실 등급" → 국외 조항(제25조)이 상위, 국내 제13조/별표1은 top3 밖 — 동일한 한계

**알려진 한계**: 이 규정은 국내(제2장)·국외(제3장) 조항이 같은 용어("운임의 지급", "등급구별" 등)를 거의
그대로 반복하는 구조라, 질의에 "국내"를 명시하지 않으면 순수 키워드 검색이 국외 조항을 더 높게 매길 수
있다. 다만 Phase 4(실비 판정 에이전트)는 이 범용 검색에 의존하지 않고 `rules/domestic_travel_rules.json`에
이미 고정된 조항 번호를 직접 사용하므로 실제 판정 로직에는 영향 없음 — 이 검색 API는 어디까지나 사람이나
에이전트가 규정 내용을 자유질의로 찾아보는 보조 도구.

### Phase 4 — 검증 로직 + 실비 판정 에이전트 통합 ✅ 완료 (2026-07-20)
- **설계 원칙 재확인**: 판정에 필요한 숫자·등급·키워드 대조는 전부 결정론적 TypeScript 코드
  (`src/lib/verifyReceipt.ts`)로 구현. OCR 단계(Phase 2)에서 이미 LLM이 이미지→텍스트 변환이라는
  "정성적 해석"을 마쳤으므로, 판정 단계에서 LLM을 다시 부르지 않는다 — 추가 LLM 호출은 지연(7~25초)과
  무료 티어 429 리스크만 늘릴 뿐, 이미 텍스트로 변환된 결과를 규칙과 대조하는 데는 필요 없다고 판단
- 조식: 품목 확인→시간(출장 기간 중 05:00:00~10:00:59)→불인정 품목 키워드→영수증 매수(사업자번호/합계
  키워드 2회 이상 등장 시 "한 사진에 영수증 2장" 휴리스틱)→금액상한(15,000원, 부분인정) 순 fail-fast
- 교통: 선박(1등석/1등실/특실)·항공(비즈니스/퍼스트) 등급 키워드 매칭
- 숙박: 영수증/receipt 키워드 확인→장소 일치(도착지·경유지 문자열이 OCR 텍스트에 포함되는지)→금액상한
  (120,000원, 부분인정)
- OCR을 아예 못 읽은 경우(`ocrStatus !== "DONE"`)는 모든 항목에서 공통으로 "인식 불가 → 불인정" 처리 —
  원 스펙에는 없던 안전장치이지만, 읽지 못한 영수증을 묵시적으로 승인하는 것을 막기 위해 추가
- Prisma `Receipt`에 `verdictStatus/verdictAmount/verdictMessage/verdictFailedCheck/verdictRegulationRef`
  추가. UI(`ReceiptManager`)에 인정/부분인정/불인정 배지 + 메시지 + 근거 조항을 배너로 표시, 허브
  화면(`trip/[id]/page.tsx`)에 카테고리별·전체 인정금액 합계 표시(요청하신 "최종 실비정산 값 자동 산출"
  에 해당)

**검증 완료 (Phase 5 시나리오 선반영)**: 아래 14개 케이스를 합성 영수증으로 실제 업로드→OCR→판정까지
end-to-end 테스트, 전부 기대값과 일치.

| 시나리오 | 기대 결과 | 실제 결과 |
|---|---|---|
| 조식 정상(4,500원, 07:35) | 인정 | ✅ APPROVED 4,500원 |
| 조식 금액초과(25,000원) | 부분인정 15,000원 | ✅ PARTIAL 15,000원 |
| 조식 시간초과(13:20) | 불인정 | ✅ REJECTED |
| 조식 불인정품목("생맥주") | 불인정 | ✅ REJECTED |
| 조식 영수증 2장 병합(사업자번호 2개) | 불인정 | ✅ REJECTED |
| 선박 일반실 | 인정 | ✅ APPROVED 45,000원 |
| 선박 1등실 | 불인정 | ✅ REJECTED |
| 항공 이코노미 | 인정 | ✅ APPROVED 80,000원 |
| 항공 비즈니스 | 불인정 | ✅ REJECTED |
| 숙박 정상(115,000원, 부산) | 인정 | ✅ APPROVED 115,000원 |
| 숙박 금액초과(180,000원) | 부분인정 120,000원 | ✅ PARTIAL 120,000원 |
| 숙박 영수증 아님(메모 텍스트) | 불인정 | ✅ REJECTED |
| 숙박 장소불일치(부산 영수증 vs 대구 출장) | 불인정 | ⚠️ 최초 실패(아래) → 수정 후 ✅ REJECTED |

**버그 발견 및 수정**: 장소 일치 검증을 단순 `text.includes(location)`으로 구현했더니, "대구"라는
지명이 "**해운대구**"(부산 해운대**구**, 행정구역 접미사 '구')의 부분 문자열로 우연히 매칭되어 대구
출장에 부산 숙박 영수증을 냈는데도 통과하는 오탐이 발생했다. 지명 문자열 시작 위치의 바로 앞 글자가
한글이면(= 더 긴 다른 단어 중간이라는 뜻) 매칭에서 제외하는 `containsLocationToken()`으로 수정해
해결(`src/lib/verifyReceipt.ts`). 뒷글자는 검사하지 않는데, "부산광역시"처럼 지명 뒤에 접미사가 바로
붙는 것은 정상이기 때문이다. 재테스트로 정상 매칭(부산↔부산)과 오탐 차단(대구↔해운대구) 둘 다 확인.

### Phase 5 — 검증 스위트 (필수 게이트) ✅ 완료 (2026-07-20)
- Vitest 도입 (`vitest.config.ts`, `npm test` = `vitest run`), `tsconfig.json`의 `@/*` 경로 별칭도 동일하게
  맞춰 별도 설정 없이 `src/lib` 모듈을 그대로 import
- `src/lib/verifyReceipt.test.ts`: Phase 4에서 실제 이미지로 수동 검증했던 시나리오를 고정 OCR 텍스트
  fixture로 옮겨 LLM/서버/네트워크 호출 없이 순수 판정 함수(`verifyBreakfast`/`verifyTransport`/
  `verifyLodging`)만 직접 테스트 — 조식 6개(정상/금액초과/시간초과/불인정품목/영수증2장/OCR실패),
  교통 5개(선박 일반석·1등실, 항공 이코노미·비즈니스, OCR실패), 숙박 6개(정상/금액초과/영수증아님/
  **장소불일치 회귀테스트**/정상매칭 회귀테스트/OCR실패) = 총 17개
- 날짜 fixture는 `new Date(naiveLocalString).toISOString()`로 생성해, 인코딩(테스트)과 디코딩
  (`verifyBreakfast` 내부의 로컬 `getHours()` 비교)이 같은 방식으로 로컬 타임존을 쓰도록 맞춤 — 실행
  환경의 타임존이 달라져도(로컬 PC든 다른 서버든) 결과가 항상 같도록 함
- Phase 4에서 고친 "대구/해운대구" 지명 오탐 버그는 **회귀 테스트로 고정**해뒀기 때문에, 이후 누군가
  `containsLocationToken` 로직을 실수로 되돌리거나 깨뜨리면 테스트가 바로 잡아낸다

**실행 결과**: `npm test` → **17개 전부 통과, 282ms**. curl 기반 수동 검증(업로드마다 OCR 대기 7~26초,
전체 몇 분 소요)과 달리 몇백 ms 안에 반복 재검증 가능 — 앞으로 판정 로직을 건드릴 때마다 이 스위트로
빠르게 회귀 여부를 확인할 수 있다. 이것으로 "사람 재확인 없이 자동 확정"이라는 2장의 전제가 일회성
확인이 아니라 지속적으로 재확인 가능한 상태가 되었다.

### Phase 6 — 결과 출력 (PDF) ✅ 완료 (2026-07-20)
- `pdf-lib` + `@pdf-lib/fontkit`로 구현 (Puppeteer 등 헤드리스 브라우저 의존성 없이 순수 JS로 처리,
  로컬 프로토타입에 가벼운 선택)
- `src/lib/generatePdf.ts`: 페이지 커서를 직접 관리하는 얇은 레이아웃 헬퍼(`PdfWriter`)로 출장정보 →
  항목별 합계 → 전체 합계 → 항목별(조식/교통/숙박) 세부내역(상호명/일시/인식금액/인정금액/판정
  메시지/근거 조항) + 첨부 영수증 이미지를 순서대로 그리고, 공간이 부족하면 자동으로 새 페이지 추가
- `GET /api/trips/[id]/pdf`: PDF를 `application/pdf` + `Content-Disposition: attachment`로 응답,
  허브 화면에 "정산 결과 PDF 다운로드" 버튼(순수 `<a href download>`, 별도 클라이언트 JS 불필요) 추가
- 이미지 임베드는 PNG/JPG만 지원(pdf-lib 자체 한계) — webp/heic는 "미리보기 미지원" 안내 텍스트로 대체
  하고 PDF 생성 자체는 계속 진행(첨부 누락으로 전체 생성이 실패하지 않도록)

**⚠️ 알려진 제약(열린 이슈로 별도 기록)**: 한글 렌더링을 위해 `C:\Windows\Fonts\malgun.ttf`(맑은 고딕)를
OS에서 직접 읽어 embedding한다 — 지금의 "로컬 Windows 프로토타입"에서는 문제없지만, 이 코드를 다른 OS나
서버로 옮기면 그 경로가 없어 PDF 생성이 실패한다. 배포 단계로 넘어가면 오픈소스 한글 폰트(예: Noto Sans
KR, SIL OFL 라이선스)를 프로젝트 자산으로 번들링하는 방식으로 교체해야 한다(마이크로소프트 폰트 파일은
라이선스상 리포지토리에 커밋해 배포하기 부적절).

**검증 완료**: 조식·교통(선박)·숙박 각 1건씩 등록된 테스트 출장으로 실제 PDF 생성 → 육안 확인.
한글(상호명·주소·메시지) 정상 렌더링, 2페이지로 자동 분할, 영수증 이미지 3장 모두 정상 첨부, 항목별/
전체 합계·판정 배지·근거 조항 전부 정확하게 표시됨. 근거 조항 텍스트에 정정 이력까지 노출되던 것을
발견해 `rules/domestic_travel_rules.json`의 `breakfast.regulation_ref`를 "별표 제1호 비고5)"로
간결화(정정 이력은 이 PRD와 git 히스토리에만 남기고, 사용자용 문서에는 결과만 보여주는 것이 맞다고 판단).

### Phase 6.1 — 사용자 피드백 5건 반영 ✅ 완료 (2026-07-20)
Phase 6(PDF) 완료 후 실제 사용 관점에서 나온 수정 요청 5건을 반영했다.

1. **출장목적 필드 삭제** — Prisma `Trip.purpose` 컬럼 자체를 마이그레이션으로 제거하고, API/폼/홈·허브
   화면/PDF에서 관련 코드를 전부 정리. 안 쓰는 필드를 옵션으로 남겨두지 않고 완전히 제거.
2. **출발/도착 날짜 연도 4자리 문제** — 원인 조사 결과 `formatDate()`(표시용)는 원래도 4자리였고,
   문제는 `TripForm`의 네이티브 `<input type="date">`가 OS/브라우저 로케일 설정에 따라 연도를 2자리로
   보여줄 수 있다는 점(내부 값은 항상 4자리, 화면 렌더링만 로케일 종속)이었다. 근본적으로 통제하기 위해
   `src/components/DateYMDInput.tsx`를 새로 만들어 년(4자리 강제)/월/일을 분리된 입력으로 받는 방식으로
   교체 — 어떤 OS/브라우저에서도 항상 4자리 연도만 표시되고 값도 4자리를 다 입력해야 완성된다.
3. **전 항목 출장기간 날짜 검증** — 기존에는 조식만 (시간대 검사에 곁들여) 출장기간을 확인했고 교통·숙박은
   전혀 확인하지 않았다. `verifyReceipt.ts`에 공통 `isWithinTripDateRange()` 헬퍼를 만들어 조식/교통/숙박
   전부 "영수증 날짜가 출장기간(출발~도착일) 안에 있는지"를 확인하도록 확장. 조식은 날짜 확인과 05:00~
   10:00:59 시간대 확인을 별개 체크로 분리(이전엔 하나로 뭉쳐 있었음).
4. **교통 항목 업체유형 확인** — 기존엔 선박/항공 모두 "좌석 등급"만 검사해서, 숙박이나 조식 영수증을
   교통 카테고리에 잘못 올려도 등급 관련 키워드가 없으면 그냥 통과(인정)되는 구멍이 있었다. 선박은
   "선박/여객선/페리/승선/항구" 등, 항공은 "항공/탑승권/공항/비행기" 등 `required_keywords`가 하나도
   없으면 `not_transport_receipt`로 불인정하도록 추가 — 등급 검사보다 먼저 수행한다.
5. **조식·교통에도 장소 검증 추가** — 기존엔 숙박만 장소(도착지/경유지) 일치를 확인했다. 조식·교통에도
   같은 매커니즘(오탐 방지용 `containsLocationToken` 재사용)으로 장소 검증을 추가했다. 다만 조식·교통은
   **출장경로 전체(출발지 포함)** 를 후보로 쓰고, 숙박은 원래 스펙대로 도착지·경유지만 후보로 쓴다(출발지
   에서 묵을 이유는 통상 없다고 보아 유지).

**검증 완료**: `verifyReceipt.test.ts`를 24개 테스트로 확장(장소불일치·기간밖날짜·업체유형불일치 케이스
추가)해 `npm test` 전부 통과 확인. 추가로 실제 이미지 업로드로 end-to-end 재확인: (a) 대전 조식 영수증을
서울-대구 출장에 올리면 `location_mismatch`로 정확히 불인정, (b) 부산-제주 선박 승선권을 서울-대구
출장에 올리면 역시 `location_mismatch`로 불인정, (c) 출장기간 밖 날짜(7/25, 출장은 7/21~7/22)의 조식·
선박 영수증이 각각 `trip_date_mismatch`로 정확히 불인정, (d) 숙박 영수증을 교통-선박 카테고리에 잘못
올리면 `not_transport_receipt`로 정확히 불인정됨을 확인.

### Phase 6.2 — 날짜피커 복원 · PDF 영수증 지원 · 금액 오인식 수정 ✅ 완료 (2026-07-20)
Phase 6.1 이후 실제 사용 중 발견된 이슈 3건을 반영했다.

1. **달력 피커 복원** — Phase 6.1에서 연도 4자리를 강제하려고 네이티브 `<input type="date">`를
   년/월/일 텍스트 입력으로 바꾸면서, 브라우저가 제공하던 달력 아이콘도 같이 사라지는 부작용이 있었다.
   `src/components/DateYMDInput.tsx`에 화면에 보이지 않는 `<input type="date">`를 하나 더 두고, 달력
   버튼 클릭 시 `showPicker()`로 그 네이티브 피커만 열어 날짜를 고르면 결과를 년/월/일 입력에 반영하는
   방식으로 절충 — 4자리 연도 강제와 달력 선택 UX를 모두 유지.
2. **PDF 영수증 지원** — 이메일로 받는 항공권/호텔 예약확인서 등 PDF 파일을 "사진 선택"에서 올릴 수
   있도록 `mupdf`(WASM, 순수 JS라 poppler 같은 OS 네이티브 도구 설치가 불필요)를 추가해 업로드 시
   PDF 첫 페이지를 PNG로 렌더링한 뒤 기존 OCR/판정 파이프라인에 그대로 태운다
   (`src/lib/pdfToImage.ts`). 파일 선택 input의 `accept`에 `application/pdf` 추가, PDF 선택 시
   미리보기는 이미지 대신 문서 아이콘 placeholder로 표시(브라우저 `<img>` 태그로는 PDF를 못 그리므로).
3. **금액 오인식 버그 수정** — 실사용 중 조식 영수증 실제 금액(8,800원)이 529,942원으로 완전히 잘못
   인식되는 사례 발견. 원인은 API 자체 문제가 아니라 `receiptHints.ts`의 정규식 휴리스틱("합계/총액
   키워드 줄에서 가장 큰 숫자")이 사업자번호·카드승인번호처럼 실제 결제액보다 훨씬 큰 숫자를 집어버린
   것 — Google Vision(유료)으로 바꿔도 원문 텍스트만 나올 뿐 그 다음 파싱은 동일한 정규식이 하므로
   재현됐을 문제였다. `src/lib/receiptOcr.ts`의 프롬프트를 [원문 전사] + [요약 JSON(상호명/금액/일시)]
   두 섹션으로 바꿔, 문맥을 이해하는 LLM이 "실제 결제된 최종 합계 금액"을 직접 구조화해 답하도록
   변경(사업자번호·카드승인번호·전화번호·포인트는 금액이 아니라고 프롬프트에 명시). 구조화 추출 결과를
   `ocrAmountGuess`/`ocrMerchantGuess`/`ocrDateGuess`의 1순위로 쓰고, 파싱 실패 시에만 기존 정규식
   휴리스틱으로 대체(안전망 유지).

**검증 완료**: 사용자가 보고한 시나리오를 그대로 재현하는 합성 PDF 영수증(실제 합계 8,800원 +
"카드승인번호 529942"라는 decoy 큰 숫자 포함)을 만들어 PDF로 업로드 → PNG 변환 → OCR → 판정까지
end-to-end 테스트, **정확히 8,800원으로 인식되고 인정됨**을 확인. `npm test` 24개 회귀 테스트도 계속
통과.

### Phase 6.3 — 다중 페이지 PDF 인식 ✅ 완료 (2026-07-21)
Phase 6.2에서 PDF 지원을 추가할 때 첫 페이지만 렌더링해서 보냈는데, 실사용 중 "PDF 여러 장을 올리면
뒷장은 인식이 안 된다"는 문제가 발견됐다(예: 1p 항공권 요약 + 2p 결제 상세내역처럼 실제 금액이
뒷장에만 있는 문서).

- `src/lib/pdfToImage.ts`에 `renderAllPdfPagesToPng()` 추가 — PDF의 모든 페이지(최대 5장, 일정표처럼
  페이지가 너무 많은 문서로 인한 지연/비용 폭증 방지용 상한)를 PNG로 렌더링
- `src/lib/receiptOcr.ts`의 `runReceiptOcr()`을 단일 이미지가 아니라 이미지 배열을 받도록 변경 —
  OpenRouter 채팅 요청 한 번에 `image_url` 블록을 페이지 수만큼 첨부해서, 모델이 문서 전체를 한꺼번에
  보고 "여러 페이지에 걸쳐 있어도 하나의 최종 금액"을 답하도록 프롬프트도 페이지 수에 따라 동적으로
  조정(원문도 "--- 페이지 N ---" 구분자로 페이지별로 옮겨 적게 함)
- 저장/썸네일 표시는 데이터 모델상 영수증당 이미지 1장이라 여전히 1페이지만 쓴다 — OCR 인식 범위만
  전체 페이지로 넓어졌다. 여러 페이지를 전부 첨부/열람하고 싶다면 Receipt가 이미지 여러 장을 가지도록
  스키마를 바꿔야 하는데, 지금은 요청받지 않아 범위 밖으로 둠(필요시 다음에 논의).
- 사이드이펙트로 발견한 사소한 버그도 같이 수정: `receiptHints.ts`의 상호명 추측 로직이 새로 생긴
  "--- 페이지 N ---" 구분선을 상호명으로 잘못 집는 문제 — 구분선 패턴을 후보에서 제외하도록 수정.

**검증 완료**: 1페이지엔 항공권 요약(금액 정보 없음), 2페이지엔 "할인 전 정가 120,000원"과
"카드승인번호 998877"이라는 decoy, 그리고 실제 "최종 결제금액 80,000원"만 있는 2페이지 합성 PDF로
테스트 — 페이지 2에만 있던 80,000원이 정확히 인식되고 인정(APPROVED)됨을 확인. 상호명 추측 버그
수정도 재업로드로 확인. `npm test` 24개 회귀 테스트 계속 통과.

**추가 검증(2026-07-21, 실사용 파일)**: 사용자가 실제 3페이지 PDF(1p 호텔 예약내역/85,000원, 2p 하이패스
통행료 영수증 2건, 3p 신용카드 매출전표/합계 79,050원)로 재현 테스트 — 3페이지 전부 인식되고 마지막
장에만 있던 실결제액 79,050원이 정확히 추출됨을 확인. 참고로 최초 실패 사례는 사후 조사 결과, 저장되는
대표 이미지가 항상 1페이지뿐이라(위 설계상 제약) 원본이 실제 다중 페이지 PDF였는지 스크린샷 한 장이었는지
기록만으로 사후 구분이 안 되는 문제가 있었음 — 당시 정황(상태바가 찍힌 스크린샷 UI, "--- 페이지 N ---"
구분자 부재)상 스크린샷 단일 이미지였거나 순간적 429 실패였을 가능성이 높았고, 파이프라인 자체의 결함은
아니었던 것으로 결론.

### Phase 6.4 — 사내망 IP로 동료 접속 허용 ✅ 완료 (2026-07-21)
사용자가 실제로 사무실 동료와 `http://10.100.3.140:3001`(이 PC의 사내망 IP)로 테스트를 시작하면서,
출장 등록 같은 입력/수정이 전혀 동작하지 않는 문제가 발생했다. 원인은 서버 문제가 아니라 **Next.js
개발서버(`next dev`)가 기본적으로 `localhost` 외 출처(origin)의 요청을 자동 차단**하는 보안 기능
때문(DNS 리바인딩 등 외부 공격 방지용) — Phase 6.1에서 로그로 이미 한 번 관찰했던 현상이 실제
장애로 나타난 것. `next.config.mjs`에 `allowedDevOrigins: ["10.100.3.140"]`을 추가해 해결.
새 동료가 다른 IP로 접속하면 이 배열에 IP를 추가해야 한다.

> ⚠️ **참고(현재 범위 밖으로 기록만)**: 이 시점부터 사실상 "로컬 단일 사용자 프로토타입"을 넘어
> 사내 동료들이 실제로 같이 쓰는 상황이 됐다. 다만 여전히 `next dev`(개발 모드)로 띄워둔 상태라
> 코드를 수정할 때마다 재시작/재컴파일이 필요하고, 그 순간 동료 쪽 화면이 잠깐 끊기거나 에러를
> 볼 수 있다. 9장에 이미 적어둔 "사내 배포 확장 시 고려사항"(인증, 직급 반영, 사람 승인 재도입,
> `next build`+`next start`로 전환 등)이 이제 더 이상 먼 미래 얘기가 아니게 됐다 — 다음에 본격적으로
> 다룰지 사용자와 논의 필요.

### Phase 6.5 — OCR 실패 시 재시도(자동+수동) ✅ 완료 (2026-07-21)
"같은 영수증인데 한 번은 정상 인식, 한 번은 '인식 불가'가 뜬다"는 문제를 조사한 결과, localhost와
IP 접속 차이가 아니라 **무료 티어 1차·폴백 모델이 동시에 업스트림 요청제한(429)에 걸린 순간에
업로드했기 때문**으로 확인됐다(로그로 확인). 동료들과 동시에 테스트하면서 요청량이 늘어 이 현상이
더 잦아졌다.

- `src/lib/receiptOcr.ts`에 `callModelWithRetry()` 추가 — 모델 호출이 실패하면 2.5초 대기 후 같은
  모델로 1회 재시도. 1차/폴백 모델 각각에 적용되어 최악의 경우 총 4회 시도 후에만 "인식 불가"로 처리
- `src/lib/analyzeReceipt.ts`로 "OCR 실행 + 힌트추출 + 항목별 판정" 로직을 분리해, 최초 업로드와
  재분석이 같은 코드를 공유하도록 리팩터링(`src/app/api/receipts/route.ts`도 이 함수를 쓰도록 정리)
- `POST /api/receipts/[id]/reanalyze` 신규 — 재업로드 없이 이미 저장된 이미지 그대로 OCR/판정만
  다시 실행. 인식 실패(`verdictFailedCheck === "ocr_unavailable"`) 상태일 때 UI(판정 배너)에 **"다시
  인식 시도"** 버튼이 나타나 바로 호출

**검증 완료**: 실제로 4회(1차×2, 폴백×2) 전부 429로 실패한 영수증에 재분석 API를 호출했더니 이번엔
정상 인식(정확한 금액·상호명)되어 인정 처리됨을 확인. `npm test` 24개 회귀 테스트 계속 통과.

> 참고: 자동 재시도가 latency를 늘린다(모델 호출이 원래도 몇~수십 초 걸리는데, 실패 시마다 2.5초씩
> 더해짐). 그래도 근본적으로는 무료 티어 용량 한계라, 사용량이 계속 늘면 유료 전환이나 다른 모델
> 조합을 다시 검토해야 할 수 있다 — PRD 열린 이슈에 이미 있는 항목.

**추가 조사(2026-07-21)**: "세로(portrait) 이미지/PDF만 인식이 안 되는 것 같다"는 제보가 있어 조사.
결론은 **방향과 무관 — 동일한 429 문제**였다. 근거: (1) 실패한 세로 PDF를 mupdf로 변환한 PNG를 직접
열어보니 정상적인 1191×1684 A4 비율 이미지(104KB)로 변환 자체는 문제없었음, (2) 같은 이미지에
재분석을 2번 더 걸어도 매번 로그에 동일한 "두 모델 다 429"만 찍힘, (3) 결정적으로 가로(900×400)와
세로(400×900) 테스트 영수증을 같은 시점에 나란히 업로드했더니 **둘 다 정상 인식**됨, (4) 사용자의
원본 세로 영수증도 "다시 인식 시도"로 재시도하니 정상 인식(70,000원, 승인)됨. 오리엔테이션에 따른
차이는 없고, 원래 영수증이 세로가 많다 보니(사진/스크린샷 특성상) 429에 걸리는 사례도 세로에서 더
자주 목격됐을 뿐인 것으로 결론.

### Phase 7 — 이메일 발송 (선택 기능) ✅ 코드 완료, 실제 발송은 비활성 상태 (2026-07-21)
"여러 직원이 쓰는 목적에 맞는 구현이 가능한가"라는 질문에서 시작해, 발신 계정 설계를 먼저 정리했다:

- **아키텍처**: SMTP 계정은 서버(`.env`)에 하나만 설정 — 누가 쓰든 같은 발신 계정을 거치고,
  **받는 사람만 매번 버튼을 누를 때 직접 입력**(요청받은 방식). 즉 여러 직원이 각자 자기 메일로
  받을 수 있는 구조는 이미 이 설계로 충족된다.
- **발신 계정 이슈**: 개인 Gmail을 연동하면 실제로 모든 발송 메일이 "그 개인 이메일에서 온 것"으로
  다른 직원들에게 노출된다는 문제를 사용자가 직접 짚어냈다 — 여러 사람에게 반복 발송 시 스팸 필터
  위험, 회신이 개인 메일함으로 감, 무엇보다 사적인 이메일 주소가 업무용 문서 발신자로 노출되는 문제.
  결론: **개인 계정으로 지금 당장 켜지 않고, 코드만 완성해두고 회사(KIOST) SMTP 계정이 나오면
  `.env`만 채워서 켜는 방식**으로 결정.
- `src/lib/sendEmail.ts`: `nodemailer` 기반. `isEmailConfigured()`가 `SMTP_HOST/USER/PASS/FROM_EMAIL`
  중 하나라도 비어있으면 기능 전체를 비활성 상태로 취급 — 코드 수정이나 재배포 없이 `.env` 값만
  채우면 다음 요청부터 바로 켜진다.
- `POST /api/trips/[id]/email`: 이메일 형식 검증 → 미설정 시 503, PDF 생성(`generateTripPdf` 재사용,
  Phase 6과 동일 함수) → SMTP 발송 → 실패 시 502.
- UI(`EmailSendButton.tsx`): PDF 다운로드 버튼 옆에 배치. `enabled=false`(현재 기본값)일 때는 "이메일
  발송 기능은 준비 중입니다(회사 SMTP 계정 연동 후 제공 예정)" 안내만 보여주고 입력 폼 자체를 숨김 —
  써지지도 않는 기능으로 사용자를 혼란스럽게 하지 않기 위함.

**검증 완료**: 미설정 상태에서 `POST /api/trips/[id]/email` 직접 호출 → 503 + 명확한 안내 메시지,
실제 이메일은 발송되지 않음(PDF 생성 단계까지 가지 않고 조기 차단) 확인. 허브 화면에도 "준비 중"
안내가 정상 표시됨을 확인. `npm test` 24개 회귀 테스트 계속 통과.

> **다음에 할 일**: 회사 SMTP 정보(호스트/포트/계정/비밀번호/발신주소)를 받으면 `.env`의 `SMTP_*`
> 값만 채우면 된다 — 코드 변경 불필요.

### Phase 8 — 출장 진행/종료 흐름 + 홈 화면 재구성 ✅ 완료 (2026-07-22)
"실제 사용 목적(출장 중 그때그때 영수증 촬영 → 종료 후 한번에 PDF/이메일)에 맞게 흐름을 바꾸자"는
요청으로 진행. 기존엔 영수증이 1건이라도 있으면 언제든 PDF/이메일 버튼이 보였는데, "출장 중"과
"출장 종료"를 명확히 구분해달라는 요청과, 출장이 연장·단축될 수 있으니 시작 후에도 경로를 고칠 수
있어야 한다는 요청을 반영했다.

- Prisma `Trip`에 `status`(`ACTIVE`/`COMPLETED`, 기본값 `ACTIVE`) 추가
- `PATCH /api/trips/[id]` 신규 — `{status}` 또는 `{stops}`(또는 둘 다) 를 받아 상태 변경과
  경로 수정을 처리. 경로 수정은 개별 정거장을 diff하지 않고 **기존 정거장을 전부 지우고 새 목록으로
  다시 생성**하는 전체교체 방식 — 정거장을 참조하는 다른 테이블이 없어 안전하고, 연장/단축/경유지
  추가삭제를 한 번에 단순하게 처리 가능
- `TripForm.tsx`를 신규 등록/기존 출장 수정 겸용으로 리팩터링(`tripId` prop 유무로 모드 분기),
  제출 버튼도 "출장 시작"(신규) / "변경 사항 저장"(수정)으로 구분
- `TripRouteSection.tsx`(신규, client) — 평소엔 경로를 읽기 전용으로 보여주고 "출장 정보 변경"
  버튼을 누르면 그 자리에서 `TripForm`(수정 모드)으로 전환. 저장/취소하면 다시 읽기 전용으로 복귀
- `TripStatusButton.tsx`(신규, client) — 진행중이면 "출장 종료" 버튼, 종료됐으면 "다시 진행중으로
  되돌리기" 링크. 되돌리기를 항상 열어둬서 실수로 종료해도 되돌릴 수 있게 함(단방향 잠금 없음)
- 허브 화면(`trip/[id]/page.tsx`): 상단에 진행중/종료됨 배지 표시. **진행중일 때는 PDF/이메일 섹션을
  아예 숨기고** 인정 합계만 담백하게 보여주며 "출장 종료를 누르면 정산 결과를 받을 수 있다"고 안내.
  **종료됐을 때만** PDF 다운로드 + 이메일 발송 섹션이 나타남
- 홈 화면(`page.tsx`) 재구성 — "진행중인 출장" 섹션을 최상단에 배치하고, 각 진행중 출장 카드에
  조식/교통/숙박 **바로가기 아이콘 버튼**을 달아 허브 화면을 거치지 않고 한 번에 촬영 화면으로 이동
  가능. 출장 등록 폼은 그 아래, 종료된 출장은 "종료된 출장" 섹션으로 분리해 맨 아래 배치

**검증 완료**: 신규 출장 생성 시 `status: "ACTIVE"` 확인 → 허브에 "진행중" 배지·PDF 섹션 없음·"출장
정보 변경"/"출장 종료" 버튼 확인 → `PATCH .../stops`로 도착일 연장 + 경유지 추가 후 허브에 정상 반영
확인(최초 시도는 Bash에 한글을 직접 넣어 인코딩이 깨졌었는데, 이전에도 겪었던 테스트 방식의 문제였고
UTF-8 파일로 다시 보내니 정상 처리됨을 확인 - 앱 버그 아님) → `PATCH .../status`로 종료 처리 →
허브가 "종료됨" 배지·PDF 다운로드·이메일 섹션·"다시 진행중으로" 링크로 정상 전환됨을 확인 → 홈
화면에서도 해당 출장이 "진행중인 출장"에서 "종료된 출장"으로 정상 이동함을 확인. `npm test` 24개
회귀 테스트 계속 통과.

> **대화 중 별도로 논의된 사안(구현 안 함, 기록만)**: "여러 직원이 쓰면 홈 화면에 남의 출장까지
> 다 보이는 게 사생활 문제 아니냐"는 질문이 나왔다. 맞는 지적이며, 지금은 로그인 개념이 전혀 없어
> 이 서버에 접근 가능한 누구나 모든 출장 목록·영수증 이미지(URL만 알면 인증 없이 직접 접근 가능)를
> 볼 수 있는 상태다. 실제로 영수증 이미지 중 하나에 예약자 이름·휴대폰번호가 그대로 노출된 사례도
> 확인됨. 사용자가 "나중에 검토"하기로 결정 — 9장 "사내 배포 확장 시 고려사항"의 인증 항목과 직결.

### Phase 8.1 — 출장 삭제/취소 기능 ✅ 완료 (2026-07-22)
"종료된 출장에서는 삭제, 진행중인 출장에서는 취소" 요청 반영. 동작은 완전히 동일(출장+정거장+영수증
DB 레코드 삭제, cascade)하고 **라벨만** 상태에 따라 다르다 — 진행중이면 "취소", 종료됐으면 "삭제".

- `DELETE /api/trips/[id]` 신규 — `prisma.trip.delete()`로 정거장/영수증 레코드는 cascade 삭제되고,
  업로드된 영수증 이미지 폴더(`public/uploads/<tripId>/`)도 함께 지워 고아 파일이 안 남게 함
- `TripDeleteButton.tsx`(신규, client) — 되돌릴 수 없는 작업이라 `window.confirm()` 확인창을 거침.
  홈 화면의 "진행중인 출장"/"종료된 출장" 카드와 허브 화면 하단에 배치. 카드 전체가 `<Link>`로
  감싸여 있는 완료 출장 카드는 버튼이 링크 안에 중첩되지 않도록 구조를 살짝 바꿈(Link는 본문만
  감싸고 삭제 버튼은 형제 요소로 분리)
- 허브 화면에서 삭제하면 그 출장 자체가 없어지므로 홈으로 리다이렉트(`redirectTo="/"` prop)

**검증 완료**: 테스트 출장 생성 → 영수증 1건 업로드(업로드 폴더 생성 확인) → `DELETE` 호출 →
DB에서 출장·영수증 레코드 모두 사라짐 확인 + 업로드 폴더도 파일시스템에서 완전히 삭제됨 확인.
홈 화면에 진행중 카드는 "취소", 종료된 카드는 "삭제" 라벨이 각각 정확히 표시됨을 확인. `npm test`
24개 회귀 테스트 계속 통과.

### Phase 8.2 — 출장기간/경로 분리 (복귀일정 반영) ✅ 완료 (2026-07-22)
사용자 질문("복귀일정은 자동으로 인식되는건가?")에서 발견된 데이터 모델 문제 수정. 기존에는 경로의
각 지점(출발/경유/도착)마다 날짜를 따로 받았는데, "도착일자"가 "목적지에 도착한 날"인지 "최종
복귀한 날"인지 모호했다 — 여러 날 머무는 출장에서 도착일자를 목적지 도착일로 입력하면, 복귀
당일에 쓴 영수증이 출장기간 범위 밖으로 판정되어 불인정될 위험이 있었음.

사용자 제안대로 **"출장기간"(시작일~종료일, 복귀 완료일까지 포함)과 "경로"(출발지·경유지·목적지,
날짜 없이 장소만)를 완전히 분리**하는 구조로 변경:

- `prisma/schema.prisma` — `Trip`에 `startDate`/`endDate` 추가, `TripStop`에서 `date` 컬럼 제거
  (경로 지점은 이제 장소만 가짐). 마이그레이션(`20260722080612_restructure_trip_date_range`)은
  기존 `TripStop.date`의 최소/최대값으로 `Trip.startDate`/`endDate`를 백필한 뒤 컬럼 재정의 —
  기존 실사용 출장 데이터 손실 없이 이전됨(검증: 부산→제주 출장의 기간이 그대로 보존됨을 확인)
- `verifyReceipt.ts`/`analyzeReceipt.ts`는 애초에 `tripStartDate`/`tripEndDate`를 별도 값으로
  받고 있어 수정 불필요 — 호출부(`receipts` POST/reanalyze 라우트)만 `trip.startDate`/`trip.endDate`를
  그대로 넘기도록 변경
- `TripForm.tsx` — 상단에 "출장기간" 시작일~종료일 입력(라벨: "복귀 완료일까지 포함"), 아래 경로
  목록은 장소만 입력. 배지 라벨도 "도착"→"목적지"로 변경해 날짜 없는 순수 위치 개념임을 명확히 함
  (`STOP_TYPE_LABEL`도 동일하게 변경, PDF/화면 전반에 반영)
- `TripRouteSection.tsx`/홈 화면/PDF(`generatePdf.ts`) — 출장기간을 한 번만 표시하고, 경로는
  날짜 없이 지점 목록만 표시하도록 변경

**검증 완료**: 대전(출발)→부산(목적지), 출장기간 7/10~7/13(목적지엔 7/10 도착하지만 7/13 복귀)로
테스트 출장 생성 → DB에 `startDate`/`endDate`가 정확히 7/10~7/13으로 저장됨을 확인(예전 방식이면
"도착일자"가 7/10으로 남아 7/13 복귀 영수증이 불인정됐을 케이스). `npx tsc --noEmit`/`npm test`
24개 통과, 홈·허브 화면 렌더링 확인 후 테스트 출장 삭제.

- **보안**: `.env`의 API 키들은 평문 상태 — 저장소 커밋 금지 (`OPENROUTER_API_KEY`가 현재 실제 사용 중인 키)
- **개인정보**: 영수증 이미지에 카드번호 일부 등 포함 가능 — 로컬 보관, 외부 공유 주의
- **정확성 우선순위**: 숫자·등급 판정은 룰테이블 기준으로 결정론적으로 처리, LLM은 텍스트 해석·설명 보조
- **감사 추적**: 판정 결과에 항상 "적용된 룰테이블 버전 + 실패/통과 단계"를 함께 저장

---

## 8. 범위 밖 (Out of Scope, 이번 단계)

- 국외여비, 이전비, 가족여비, 승선(항해)출장 여비
- 직급별 차등 한도 적용
- 교통 항목 중 철도/자동차(버스·자가운전) — 이번 스코프는 선박/항공만
- 결재선 연동, 회계 시스템 연동, 사내 인트라넷 출장시스템 연동
- 사내 다중 사용자 배포 인프라 (9장은 참고용)

## 9. (참고) 향후 사내 배포로 확장할 경우 고려사항

- API 키를 클라이언트에 내려주지 않는 서버/브라우저 분리 구조를 그대로 유지
- 직원별 직급 정보를 반영해야 하는 경우 별표1 전체 등급(임원/책임급/선임급이하) 재도입 검토
- 규정 위반/한도초과 건에 대해 런타임 사람 승인 단계(담당 부서 확인)를 다시 넣는 것을 권장 — 여러 사용자의
  실제 예산 집행이 걸리므로 로컬 프로토타입 때보다 감사 리스크가 커짐
- 사용자 인증(사내 SSO 등), 접근 로그, 무료 LLM 티어 요청 제한(분당/일일 한도) 고려한 유료 전환 검토

---

## 10. 열린 이슈 (착수 전 확인 권장)

1. ~~숙박비 상한 120,000원 vs 규정 PDF 별표1 추정치 80,000원~~ — PDF 별표1 재추출 결과 "(상한액: 80,000)"이
   책임급·선임급이하 병합 셀로 확인되어 규정 원문은 80,000원 쪽에 무게가 실리나, **사용자가 120,000원을
   최종 확정**. `rules/domestic_travel_rules.json`의 `known_discrepancies`에 근거 기록 완료.
2. ~~`OPENROUTER_API_KEY` 미발급~~ — 발급 및 `.env` 반영 완료. `google/gemma-4-31b-it:free` 모델이
   업스트림(Google AI Studio)에서 **거의 상시 429(요청 제한)** 를 반환하는 것을 Phase 0~2에서 반복 확인
   (2026-07-20 기준). Phase 2에서 `gemma-4-26b-a4b-it:free` 자동 폴백을 구현해 실사용에는 지장이 없게
   조치했으나, 1차 모델이 계속 혼잡하면 매 요청이 폴백까지 거쳐가며 지연(테스트 기준 건당 7~26초)이
   발생한다 — 트래픽이 늘면 유료 전환/폴백 우선순위 조정을 검토할 것.
2-1. ~~Google Vision 결제 미활성화(403)~~ — 사용자가 결제 활성화를 보류하기로 결정, **Vision 자체를
   제거**하고 이미 설정된 멀티모달 LLM으로 OCR을 대체(Phase 2). `GOOGLE_VISION_API_KEY`는 더 이상 사용되지
   않는 값으로 `.env`에 남아있음.
3. 조식 4번 검증(영수증 매수 확인)을 1~3번보다 먼저 수행할지 여부 — 현재는 사용자가 준 순서(1→5) 그대로 반영
4. 숙박 2번 검증 실패 메시지 문구를 원문 그대로 유지할지, 다듬은 버전을 쓸지
5. 이메일 발송 기능을 위한 SMTP 계정 정보(발신 주소, 서버) 확보 필요 (Phase 7)
